# bharatanatyam-mudra-classification

## Prerequisites

You will need `uv` to handle dependencies:

https://docs.astral.sh/uv/#installation

Then to use the packages needed, run:

```
uv sync
```

And don't forget to set the interpreter as the Python in the `.venv` folder created by uv.

(I am using VS Code on Windows)